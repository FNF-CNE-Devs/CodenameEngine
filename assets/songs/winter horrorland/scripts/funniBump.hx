function beatHit(curBeat:Int) {
	switch(curBeat) {
		case 120:
			camZoomingInterval = 2;
		case 153:
			camZoomingInterval = 4;
		case 216:
			camZoomingInterval = 2;
		case 230:
			camZoomingInterval = 4;
	}
}